Fix all clippy warnings

Proc-macro crate:
- Collapse nested if statements using let-chains (collapsible_if)
- Use is_some_and instead of map_or(false, ...) (unnecessary_map_or)
- Change Vec<Box<Type>> to Vec<Type> (vec_box - Box is redundant)
- Collapse nested match expressions (collapsible_match)

Main example and tests:
- Add Default implementations for types with pub fn new() (new_without_default)
